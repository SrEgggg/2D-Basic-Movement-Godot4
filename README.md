# 2D-Basic-Movement-Godot4
note: Add this in the Project Settings > Input map

Add these inputs:

up → Bind to W or ↑ arrow.

down → Bind to S or ↓ arrow.

left → Bind to A or ← arrow.

right → Bind to D or → arrow.
